export interface SuggestUpdateNodeInteface {
    reason?: string,
    name?: string,
    title?: string,
    phone?: string,
    address?: string,
    nickName?: string,
    birthDate?: Date,
    deathDate?: Date,
}

export interface SuggestDeleteNodeInteface {
    reason: string,
}
